# English Input Assistant — アプリ仕様書

自然な英語のテキスト入力を支援するデスクトップアプリ。

## アプリの役割と概要

- 自然な英語のテキスト入力を支援する
- 主要な機能は、入力された日本語または英語を、解説付きでより自然な英文に変換し、それをもとのテキストと置き換えること
- 実現したい事としてはPot [Translation & OCR](https://pot-app.com/en/) に近いが、より限定的な機能で、元のテキストを自然な英文に高速で置き換える事が一番の目的
- 対応パターンは2つのみ:
  - ユーザーが入力した日本語→LLMが提案する自然な英語
  - ユーザーが入力した英語→LLMが提案する自然な英語
- 個人利用。UIは英語のみ

## プラットフォーム・技術

- リポジトリ: https://github.com/t-hamano/english-input-assistant
- Windowsで動作することが必須要件
- Tauri v2 + Vanilla TypeScript + Vite
- LLM: Google Gemini API（gemini-2.5-flash）
- TTS: Google Cloud Text-to-Speech API（Standard voices）
- オンライン前提（オフライン動作は不要）

## 操作フロー

1. ユーザーが任意のアプリ上でテキストを選択する
2. グローバルショートカットキー（`Ctrl+Alt+Space` 固定）で変換を実行
3. アプリはクリップボード経由で選択テキストを取得する（内部的にCtrl+Xを送信）
4. LLM APIに変換リクエストを送信し、完了後に結果ポップアップを一括表示
5. ポップアップ内の「Replace」ボタンを押すと、元のアプリにフォーカスを戻し、変換後の英文をペースト（内部的にCtrl+Vを送信）
6. 置き換え後、ポップアップは自動で閉じる

### テキスト取得・置き換えの方式

- クリップボード経由（Ctrl+X / Ctrl+V をシミュレート）で実行
- Ctrl+Xで元テキストをカットし、LLM応答後にCtrl+Vで置換テキストをペースト
- Closeボタン押下時は元テキストを復元する
- クリップボードの元の内容の復元は行わない
- この方式により、Chrome、Slack、コードエディター等、あらゆるアプリのテキストフィールドで動作可能

## UI

### システムトレイ（タスクバー常駐）

- タスクバーに常駐し、設定画面へのアクセスを提供

### 設定画面

- Google APIキーの入力
- 独自の追加プロンプトを設定するためのテキストエリア（用途: トーンの指定。例:「カジュアルな表現を優先して」「ビジネスメール向けに」）
- スタートアップ時に自動起動するかどうかを設定可能

### 変換結果ポップアップ

- Tauriの独立ウィンドウとして他アプリの上にオーバーレイ表示
- ドラッグで移動可能、リサイズ可能
- 表示位置: 選択テキスト付近（マウスカーソル位置基準）。画面に収まらない場合は垂直方向にフリップ
- コンテンツに合わせて高さが自動調整（最小300px、最大600px）
- 表示内容:
  - 元テキスト
  - 変換された自然な英文
  - 基本的な文法の解説（日本語）
  - 変換元が英語の場合のみ、元の英文からの改善点の説明（日本語）
- ボタン:
  - Replace: 元のテキストを変換後の英文で置き換える。実行後ポップアップは自動で閉じる
  - Play: 変換後の英文を音声で再生（Google Cloud TTS）
  - Close（×ボタン）: 元テキストを復元してポップアップを閉じる

## LLM API

- Google Gemini API（gemini-2.5-flash）を使用
- LLMレスポンスはJSON形式で構造化して返させ、パースする（英文・文法解説・改善点を分離）
- ストリーミング不要。完了後に一括表示
- リクエスト失敗時は2〜3回自動リトライ
- リトライでも解消しない場合、ポップアップにエラーメッセージとリトライボタンを表示

## 音声再生（TTS）

- [Google Cloud Text-to-Speech](https://cloud.google.com/text-to-speech) APIを使用（Standard voices）
- LLM APIと同じGoogle APIキーを共用
- 一度再生した音声はメモリ内に一時キャッシュし、同じテキストの再リクエストを防ぐ（APIクレジット節約）
- 再生のみ。ファイルとしての保存機能は不要

## ビルド

- `npm run build; npx tauri build` でWindows用インストーラー（MSI/NSIS）を生成
- `npm run tauri dev` で開発モード起動（ホットリロード付き）

## スコープ外

- 自動アップデート（個人利用のため手動ビルド＆インストールで十分）
- オフライン動作
- 多言語UI
- 変換履歴
- 日英・英英以外の言語ペア
- 右クリックコンテキストメニュー（OS制約により、グローバルショートカットキーをメインの実行手段とする）
- 複数LLMプロバイダの切り替え（Google Gemini固定）
